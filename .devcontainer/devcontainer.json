//devcontainer.json
{
    "name": "Haskell",
  
    // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
    "image": "thewombat/dockerprog:latest",
    //"build" :  {
    //    "dockerfile" : "Dockerfile"
    //},
    "workspaceFolder": "/hs_files",
    "workspaceMount": "source=.,target=/hs_files,type=bind",
    "customizations": {
        "vscode": {
            "extensions": [
                "haskell.haskell"
            ]
        }
    }
    
  }
